---
label: Content Library
hide_body: true
fields:
- name: content
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: true
    label: Title
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: pdf
    type: file
    config:
      maxSize: 64
    label: PDF
  config:
    min: 
    max: 
    labelField: 
  label: Content
pages:
- content/data/content-library.json
